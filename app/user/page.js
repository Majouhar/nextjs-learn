import React from "react";
import { cookies, headers } from "next/headers";

function UserHome({ param, req, res }) {
  // console.log(cookies());
  // console.log(headers());
  return <div>UserHome</div>;
}

export default UserHome;
